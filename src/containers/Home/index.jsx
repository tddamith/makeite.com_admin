import React from "react";
import MenuButtonCard from "../../components/menuButton";
import { AddIcon } from "../../config/icon";

const Home = () => {
  return (
    <div className="flex flex-col gap-4">
      {/* <MenuButtonCard icon={AddIcon("layout")} content={"Template"} /> */}
    </div>
  );
};

export default Home;
